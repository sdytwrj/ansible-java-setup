on: 
  workflow_dispatch:

jobs:
  set-up:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: setup Docker Compose
        run: |
          docker-compose up
          sleep 120s
          docker-compose stop
      
    

